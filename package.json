{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cross-env npx playwright test",
    "headed": "cross-env npx playwright test --headed",
    "integration": "cross-env npx playwright test '@integration' --headed",
    "test:smoke": "cross-env npx playwright test '@smoke' --headed",
    "test:regression": "cross-env npx playwright test '@regression' --headed",
    "test:chrome": "cross-env npx playwright test --headed --project=chromium --wrokers=1",
    "test:failed": "cross-env npx playwright test --headed --last-failed",
    "test:": "cross-env npx playwright test --headed",
    "test:pw-report":"cross-env npx playwright show-report",
    "test:allure-report" : "cross-env npx allure generate allure-results --clean && npx allure open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "allure-playwright": "^3.4.1",
    "dotenv": "^17.2.2"
  },
  "dependencies": {
    "allure-commandline": "^2.34.1",
    "cross-env": "^10.0.0",
    "dot-env": "^0.0.1",
    "tesseract.js": "^5.0.1",
    "xlsx": "^0.18.5"
  }
}
